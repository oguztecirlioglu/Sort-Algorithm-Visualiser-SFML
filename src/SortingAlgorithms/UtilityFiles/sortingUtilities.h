#ifndef SORTINGUTILITIES_H_INCLUDED
#define SORTINGUTILITIES_H_INCLUDED

//include needed files
#include <vector>

namespace sortingUtils {
	void swap(int& x, int& y);
	void printArray(std::vector<int>& numsArray);
}
#endif
